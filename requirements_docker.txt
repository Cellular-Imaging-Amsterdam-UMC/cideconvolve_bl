# ===========================================================================
# Docker-specific requirements for CIDeconvolve
# ===========================================================================
# PyTorch CUDA wheels are pulled via --extra-index-url (see Dockerfile).
# All versions pinned for reproducible builds.
# ===========================================================================

# PyTorch with CUDA 12.6 support
--extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.4

# Core dependencies
numpy>=1.26,<3
tifffile>=2024.1.30
ome-types>=0.5
Pillow>=10.0

# Benchmark system metrics
psutil>=5.9
nvidia-ml-py>=12.0.0

# OME-TIFF and OME-Zarr reading
bioio==3.2.0
bioio-ome-tiff==1.4.0
bioio-ome-zarr==3.3.0
zarr>=2.16
numcodecs>=0.12
