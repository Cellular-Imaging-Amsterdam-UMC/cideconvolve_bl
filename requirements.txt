# ===========================================================================
# Dependencies for CIDeconvolve (GUI + deconvolution)
# Python 3.10 or 3.11 recommended
# ===========================================================================

# PyTorch with CUDA 12.6 support
--index-url https://download.pytorch.org/whl/cu126
torch>=2.4

# Core dependencies
numpy>=1.26,<3
tifffile>=2024.1.30
ome-types>=0.5
Pillow>=10.0

# Benchmark system metrics
psutil>=5.9

# GUI
PyQt6>=6.5.0
imageio[ffmpeg]>=2.34.0

# OME-TIFF, OME-Zarr, CZI, ND2 reading via bioio
bioio==3.2.0
bioio-ome-tiff==1.4.0
bioio-ome-zarr==3.3.0
bioio-czi==2.6.1
bioio-nd2==1.7.0

# OMERO browser + shared XYZT/3D viewer components
# Requires ZeroC ICE wheel installed separately — see README.md
omero-browser-qt[viewer]==0.2.2

# Zarr (needed for HCS plate reading/writing)
zarr>=2.16


