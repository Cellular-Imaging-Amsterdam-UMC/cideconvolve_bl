# ===========================================================================
# Dependencies for the local GUI (gui_deconvolve_ci.py + launcher.py)
# Python 3.11 recommended!!
# ===========================================================================

# PyTorch with CUDA 12.6 support
--extra-index-url https://download.pytorch.org/whl/cu126
torch>=2.4

# Core
numpy>=1.26,<3
Pillow>=10.0

# GUI
PyQt6>=6.5.0
imageio[ffmpeg]>=2.34.0
matplotlib>=3.8.0

# OME-TIFF, OME-Zarr, CZI, ND2 reading via bioio
tifffile>=2024.1.30
imagecodecs>=2024.1.1
bioio==3.2.0
bioio-ome-tiff==1.4.0
bioio-ome-zarr==3.3.0
bioio-czi==2.6.1
bioio-nd2==1.7.0

# OMERO browser (Open OMERO… button)
# Requires ZeroC ICE wheel installed separately — see README.md
omero-browser-qt[viewer]==0.2.5

# Leica browser (Open Leica… button)
leica-browser-qt>=0.2.0

# Optional — GPU / system monitoring widgets in the GUI
psutil>=5.9.0
nvidia-ml-py>=12.0.0

# Zarr (needed for HCS plate reading/writing)
zarr>=2.16
numcodecs>=0.12
